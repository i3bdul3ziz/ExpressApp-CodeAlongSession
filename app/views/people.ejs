<h1>People Page</h1>

<table>
    <tr>
        <th> First Name </th>
        <th> Last Name </th>
    </tr>
<% data.forEach(function(data, index) { %>
    <tr>
        <td><%= data.firstName %></td>
        <td><%= data.lastName %></td>
    </tr>
    <% }) %>
</table>